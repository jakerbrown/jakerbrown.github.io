Worked on a small title update for the Philip Roth post.

Added the prefix `Automation experiment I:` to the post title and rebuilt the
generated blog page so the visible site matches the source post.

This matters because the title now frames the piece more clearly as part of an
automation experiment series.

Verified by rebuilding with `python3 scripts/build_blog.py` and checking the
updated title in both the source post and `blog/index.html`.
