Worked on publishing the teaching AI post to the site.

Changed the blog source to use the new published post file and image, rebuilt the blog so the post appears on the main page, and carried forward the generator and CSS updates that make early publish timing and footnotes render correctly.

This mattered because the post was present in source form but not yet in the built site, so it would not go live on GitHub Pages without a publish commit.

Verified that the rebuilt `blog/index.html` includes the teaching post with the Superman image path and a rendered footnote block. I did not verify the live deployed site from the network in this run.
