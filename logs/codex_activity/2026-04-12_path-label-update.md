Updated the newly published two-stage confounding blog post to use "Path 1" and "Path 2" instead of "Stage 1" and "Stage 2."

Changed the post text, updated the visible labels in the linked DAG SVG files, rebuilt the blog pages, and checked the rendered HTML.

This mattered because the terminology is now consistent across the essay and its figures.

Verified that the generated blog page now shows the new wording. Remaining "Stage" strings are only internal SVG comments, not visible on the site.
