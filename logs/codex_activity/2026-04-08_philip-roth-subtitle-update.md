Worked on updating the Philip Roth post subtitle to use a fuller attribution
note.

Changed the subtitle to say that Codex handled the coding, methods decisions,
and writeup, with a placeholder prompt link, then rebuilt the generated blog
page so the visible site matches the source post.

This matters because the post now carries a much clearer disclosure about how
the work was produced.

Verified by rebuilding with `python3 scripts/build_blog.py` and checking the
updated subtitle in `blog/index.html`.
