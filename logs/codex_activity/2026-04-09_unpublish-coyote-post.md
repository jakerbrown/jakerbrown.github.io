Pulled the Belmont coyote post back out of the live blog after it was published earlier than intended.

Moved the source file out of `blog/posts-src/` into a drafts folder and rebuilt the blog so the generated public pages no longer include it.

This mattered because the user wanted the Knausgaard post live but not the coyote post yet, and the generator publishes every file in the source folder.

Verified the rebuild now produces five live posts across one page. I did not change the draft post's text, date, or content beyond moving it out of the publish path.
