Worked on a quick cleanup of the Philip Roth blog post title and subtitle.

Changed the source post to use the shorter title `Who writes like Philip Roth?`
and the simpler subtitle `AI automated research and writeup.` Then rebuilt the
blog page so the public output matches the source.

This matters because the post heading now reads more directly on the blog index
and better matches the framing you wanted for launch.

Verified by rebuilding with `python3 scripts/build_blog.py` and checking the
updated title and subtitle in both the source post and `blog/index.html`.
