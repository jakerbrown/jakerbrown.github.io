Rebuilt the site and published the current pending blog changes.

Ran the blog build to refresh generated pages, then committed the outstanding edit in the place-effects source post together with the regenerated blog index and pushed them to GitHub.

This mattered because the current blog state needed another live publish checkpoint.

Verified that the blog build completed successfully before the commit. The build updated `blog/index.html` in this pass.
