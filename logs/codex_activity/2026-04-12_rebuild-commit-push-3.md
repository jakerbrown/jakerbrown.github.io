Rebuilt the site and published the current pending website changes.

Ran the blog build to refresh generated pages, then committed the outstanding source, generated, and newly added project files that were already present in the repo and pushed them to GitHub.

This mattered because the current website state needed a fresh publish checkpoint.

Verified that the blog build completed successfully before the commit. The build updated `blog/index.html` in this pass.
