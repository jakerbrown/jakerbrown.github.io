Worked on renaming the Claude-to-Codex blog post for a cleaner series-style
presentation.

Changed the post title to `Claude v Codex I` and the subtitle to `Porting
Claude workflow to Codex`, then rebuilt the generated blog page so the visible
site matches the source post.

This matters because the post now has a shorter, more legible headline and a
clearer subtitle on the blog index.

Verified by rebuilding with `python3 scripts/build_blog.py` and checking the
updated title and subtitle in `blog/index.html`.
